<resources>
    <!-- Base application theme. -->
    <style name="Theme.RboardThemeCreator" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:colorBackground">@color/app_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent</item>
        <item name="colorPrimaryContainer">@color/accent_2</item>
        <item name="colorOnPrimary">@color/text</item>

        <item name="colorSecondary">@color/accent</item>
        <item name="colorSecondaryVariant">@color/accent</item>
        <item name="colorSecondaryContainer">@color/md3</item>
        <item name="colorOnSecondary">@color/text</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/foreground</item>
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="switchStyle">@style/Widget.App.Switch</item>

    </style>

    <style name="Widget.App.Switch" parent="Widget.Material3.CompoundButton.Switch">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.Switch</item>
    </style>

    <style name="ThemeOverlay.App.Switch" parent="Theme.RboardThemeCreator">
        <item name="colorPrimary">@color/accent_2</item>
        <item name="colorPrimaryContainer">@color/accent</item>
        <item name="colorOnSurface">@color/switch_track</item>
        <item name="colorOutline">@color/switch_thumb</item>
    </style>
</resources>